# 월드 이벤트 전달 범위 통일

- 상태: 구현 완료 — 공통 전달 계층 및 클라이언트 적용 가이드
- 작성: 2026-09-15
- 구현: 2026-09-15, 프로토콜 80
- 지형 HTTP 분리: 2026-09-16, 프로토콜 81
- 이전 구현 조사 기준: 작업 트리 `6c0df858`의 서버·shared·웹·agent-client

## 문서의 목적

월드 이벤트의 공통 전달 규칙과 구현 구조를 정리한다. 새 기능의 주변 전달을 구현·리뷰할 때 이 문서를 따른다. 공통 규칙이 바뀌면 문서도 함께 갱신한다.

§2는 리팩토링 전 조사 기록이다. §1과 §3~7은 적용한 규칙과 구현 구조이며, §6은 새 기능 구현 절차, §8은 검증 기준과 실행 결과다.

## 1. 공통 원칙과 범위

1. 주변 월드 상태의 전달 반경은 `EVENT_DELIVERY_RADIUS = 32 m` 하나로 통일한다. 웹과 agent-client에 같은 규칙을 적용한다.
2. 대상의 위치·형상·공간으로 수신자를 결정한다. 플레이어·몬스터 이동은 시작점 또는 목표점 중 하나라도 반경 안이면 전달한다.
3. 서버는 연결별로 전달한 대상 목록을 관리한다. 이를 관심 집합(AOI, Area of Interest)이라 부른다. 진입 시 최신 상태, 체류 중 변경, 이탈·삭제 시 정리를 전달한다.
4. 클라이언트의 활성 상태와 보관용 캐시를 구분한다. 재진입 시 서버의 최신 상태를 확인한다.
5. agent-client는 활성 관심 집합을 LLM 인지에도 사용한다. 별도 27 m 거리 필터와 `NPC_SIGHT_RADIUS`는 제거한다.

### 화면 가장자리 정책

32 m는 시야 범위와 네트워크 대역폭·서버 처리량 사이의 절충값이다. 넓은 화면 가장자리에서 구독 범위 밖의 변경을 받지 못하거나, 접근할 때 오브젝트·상호작용이 갑자기 나타나거나 사라지는 현상은 허용한다. 화면비·해상도별 반경 확대나 화면 전체 동기화는 요구하지 않는다.

동기화 보장은 공통 판정으로 구독하는 대상에 적용한다. 구독 중 변경 전달, 이탈 정리, 재진입 시 최신 상태 복원은 유지한다.

### 메시지 분류

| 분류 | 예 | 전달 원칙 |
| --- | --- | --- |
| 주변의 지속 상태 | 플레이어·몬스터·집·문·아이템·영지 창고·울타리·좌판·음식·모닥불·연주·낚시 | 공통 반경과 진입·변경·퇴장 처리 |
| 주변의 일회성 효과 | 공격·피격·강화 효과, 채팅 말풍선, 음표 | 효과 대상의 공통 반경. 재진입 시 과거 효과 재생 안 함 |
| 변경 가능한 지형 | 건축·조경으로 바뀌는 높이·나무·풀·조경 타일 | 해당 타일 구독자에게 갱신 |
| 정적 배치 | 편집기로 놓은 일반 가구·오브젝트 | HTTP 지역 로드·캐시 유지 |
| 개인·참여자 정보 | 명령 결과·위치 보정·인벤토리·경험치·창고 내용물·거래·귓속말·파티 상태 | 참여자·권한 기준 직접 전달 |
| 전역 서비스 | 시간·날씨·서버 공지·공개 월드 지도 | 서비스별 범위와 버전 정책 |

한 행동에 개인 결과와 주변 표현이 함께 있으면 각각 전달한다. 예를 들어 창고 배치 결과와 인벤토리는 조작자에게, 창고 외형은 구독자에게 보낸다. 몬스터 AI는 서버에서만 실행하며, 몬스터 제거는 모든 구독자에게 월드 상태 변경으로 전달한다.

공격 사거리, 상호작용 거리, 몬스터 감지·추격 거리, 음량 감쇠, GPU 렌더링 거리는 각 기능의 규칙으로 유지한다. 차단 사용자 필터도 유지한다.

### 정적 배치의 운영 전제

프로덕션에서는 편집기로 일반 가구·오브젝트를 변경하지 않는다. 이 데이터는 배포 사이에 고정된 것으로 취급한다.

- 기존 1024 m HTTP 지역 파일과 지역 캐시를 유지하며, `put_object`에 실시간 전파를 추가하지 않는다.
- 배포 데이터가 바뀌면 웹·agent-client의 캐시, 다운로드 기록, 충돌 데이터를 함께 갱신한다.
- 새 `world_epoch`에서 웹의 오브젝트 지역 캐시·가구·교량 충돌과 agent의 가구 다운로드 기록·배치·충돌을 해제한다. 이전 세대의 HTTP 응답은 적용하지 않는다. 같은 세대의 정적 지역은 재사용한다.
- 플레이어가 배치·회수하는 영지 창고·울타리·좌판과 건축·철거하는 집은 동적 대상이다. 정적 의자에 앉는 동작도 플레이어 상태로 전달한다.

## 2. 리팩토링 전 구현 기록

이 절의 범위·함수명은 `6c0df858` 조사 당시의 기록이며, 현재 전달 경로는 §5를 따른다.

### 2.1 전달·보관 범위

| 대상 | 현행 규칙 | 근거 |
| --- | --- | --- |
| 플레이어·몬스터·전투·주변 채팅 | 대부분 같은 층·XZ 반경 32 m | [world.rs](../shared/src/world.rs), [player.rs](../server/src/game_state/player.rs) |
| 플레이어·몬스터 이동 | 직전 위치와 새 확정 위치의 수신자 집합 차이로 전달. 몬스터 목표점은 계산에 미포함 | [player.rs](../server/src/game_state/player.rs) `fanout_player_position_update`, [monster.rs](../server/src/game_state/monster.rs) `fanout_monster_position_update` |
| 집 생성·변경·철거 | 집 원점 중심 32 m, 지상층 0 | [housing/routes.rs](../server/src/housing/routes.rs) `broadcast_house_change`, `delete_house` |
| 집 문·창문 | 조작자 중심 32 m, 조작자와 같은 층 | [connection.rs](../server/src/connection.rs) `ToggleDoor` |
| 웹 집 데이터 | 64 m 청크 3×3 로드, 청크 인덱스 거리 2까지 보관(최대 5×5) | [housingManager.ts](../client/src/lib/managers/housingManager.ts) `LOAD_RADIUS`, `EVICT_RADIUS` |
| agent 집 데이터 | 위치·스케줄 주변 3×3 청크를 공유 캐시에 보관, 성공한 청크는 재요청 안 함 | [driver/movement.rs](../agent-client/src/driver/movement.rs), [world_cache.rs](../agent-client/src/state/world_cache.rs) |
| 편집 가구·오브젝트 | 1024 m HTTP 지역 파일. 저장 시 서버 충돌만 갱신. 웹 충돌 캐시는 인접 지역까지, agent는 지역별 1회 수신 | [terrain/routes.rs](../server/src/terrain/routes.rs) `put_object` |
| 영지 창고·울타리 | 32 m, 초기 목록과 이동 차이·배치·회수 전달. 창고는 같은 층, 울타리는 지상층 | [estate_storage.rs](../server/src/game_state/estate_storage.rs), [fence.rs](../server/src/game_state/fence.rs), [player.rs](../server/src/game_state/player.rs) |
| 아이템·모닥불·좌판·팁 모자·음식 | 같은 층·32 m, 초기 상태와 이동 시 등장·퇴장 전달 | [player.rs](../server/src/game_state/player.rs) `aoi_diff` 및 배치·회수 코드 |
| 던전 문·소품 | 주로 32 m. 입구 문은 입구, 내부 문·소품 수동 조작은 조작자 중심. 별도 스냅샷 존재. 웹 입구는 32 m 재진입 시 문 재요청, 80 m에서 등록 해제 | [dungeon.rs](../server/src/game_state/dungeon.rs), [dungeonManager.ts](../client/src/lib/managers/dungeonManager.ts) |
| 조경 | 조작자와 타일 중심 96 m 이내는 본문, 나머지 접속자에게 무효화. 층 제한 없음 | [landscaping.rs](../server/src/game_state/landscaping.rs)의 `EVENT_DELIVERY_RADIUS + 64.0` |
| 건축으로 바뀐 높이·나무·풀 | 집 원점 중심 32 m·지상층에 타일 무효화 | [housing/routes.rs](../server/src/housing/routes.rs) `broadcast_house_change` |
| 악기 | 연주 시작 32 m, 음표 30 m | [instrument.rs](../server/src/game_state/instrument.rs) `INSTRUMENT_AUDIBLE_RADIUS` |
| 낚시 | 찌 중심 32 m | [fishing.rs](../server/src/game_state/fishing.rs) `broadcast_fishing` |
| agent 인지 | 수신 후 LLM 상태·이벤트·대상 검색에서 27 m로 재필터 | [perception.rs](../agent-client/src/state/perception.rs), [world_state.rs](../agent-client/src/state/world_state.rs), [prompt.rs](../agent-client/src/driver/prompt.rs) |

당시 주요 불일치는 집 보관 범위와 변경 수신 범위, 조작자 중심 전파, 집 외관의 같은 층 필터였다. 이관에서 삭제 전 수신자 보존, 늦은 HTTP 응답의 역전 방지, 여러 agent의 공유 캐시 수명도 함께 처리했다.

### 2.2 무효화 이후 재수신

| 메시지 / 클라이언트 | 현행 처리 |
| --- | --- |
| `HeightTilesInvalidated` / 웹 | 수신 직후 갱신 큐에 등록. 이전 높이 요청 이후 캐시를 지우고 HTTP 재요청 |
| `TreeTilesInvalidated`, `GrassTilesInvalidated` / 웹 | 높이 갱신 이후 해당 타일 재요청·표시 갱신 |
| `LandscapeChanged` / 웹·agent | 메시지 본문을 바로 적용 |
| `LandscapeInvalidated` / 웹 | 캐시 무효화. 이후 타일 로드 함수 호출 시 재요청 |
| `LandscapeInvalidated` / agent | 지표 재질 캐시 무효화. 다음 조회 시 `read_fresh`로 재요청 |
| 건축 관련 높이·나무·풀 무효화 / agent | 전용 갱신 분기 없음. 높이는 메모리에서 제거돼도 HTTP 디스크 캐시를 재사용할 수 있음 |

근거: 웹 [messageHandlers.ts](../client/src/lib/network/messageHandlers.ts), [높이](../client/src/lib/managers/terrainHeightManager.ts)·[나무](../client/src/lib/managers/terrainTreeDataManager.ts)·[풀](../client/src/lib/managers/terrainGrassDataManager.ts)의 `refreshTiles`; agent [orchestrator.rs](../agent-client/src/orchestrator.rs), [splat.rs](../agent-client/src/splat.rs), [terrain_http.rs](../agent-client/src/terrain_http.rs). 웹 높이 갱신은 미저장 편집 타일을 제외한다. 재요청 시작과 완료 시점은 구분한다.

## 3. 수신자 판정

### 3.1 반경과 형상

```rust
pub const EVENT_DELIVERY_RADIUS: f32 = 32.0;
```

[shared/src/world.rs](../shared/src/world.rs)에 정의한다. 서버·agent-client는 shared를 사용하고, 웹은 공통 `world_constants()` WASM API로 읽는다. 판정 구현은 [shared/src/interest.rs](../shared/src/interest.rs)의 `Space`, `Bounds`, `SubjectArea`에 있다.

`R = EVENT_DELIVERY_RADIUS`일 때 기본 판정은 다음과 같다.

```text
구독 대상 = 공간이 호환됨(viewer, subject)
            AND XZ_최단거리(viewer.position, subject.bounds) <= R
```

- 관찰자 위치는 서버가 확정한 캐릭터 위치다.
- 점은 중심, 선분·영역은 실제 형상까지의 최단거리로 판정하며 경계 R을 포함한다.
- X축 월드 순환을 적용한다. 점에는 `Position::dist_xz_sq`를 사용하고 선분·영역도 같은 규칙을 따른다.
- 공간 인덱스의 사각 경계는 후보 조회용이다. 여러 방 사이 빈 공간 등을 실제 형상으로 간주하지 않는다.
- 반경 제곱과 조회할 셀 범위는 R에서 계산한다. 저장 청크·인덱스 셀 크기는 최종 구독 판정에 영향을 주지 않는다.

### 3.2 층과 공간

| 대상 | 공간 조건 |
| --- | --- |
| 플레이어·몬스터·아이템·영지 창고·음식·좌판 등 | 같은 층. 지하는 던전 식별자와 깊이도 일치 |
| 집 전체·문·창문 | 집을 구독하는 지상 및 주택층 사용자. 지하 던전 제외 |
| 울타리 | 지상층 |
| 던전 입구 / 내부 문·소품 | 입구는 지상 구조, 내부는 같은 던전·깊이 |
| 지표의 높이·나무·풀·조경 | 지상 및 주택층에서 사용하는 지표 데이터. 지하 경로의 입구 구조는 별도 대상으로 표현 |

계단은 서버가 확정한 공간 전환 시점에 판정한다. 집 외관 구독으로 다른 층의 플레이어·몬스터까지 공개하지 않는다.

### 3.3 집·문·창문

집은 모든 층의 방·문·창문을 포함하는 한 동기화 단위다. 집의 실제 바닥 영역까지 거리가 R 이내면 전체 스냅샷을 받고, 구독 중에는 모든 문·창문 변경을 받는다. 집 안의 관찰자는 거리가 0이다. 반대쪽 문이 R 밖이어도 집 구독자에게 변경을 전달한다.

집 전체가 범위를 벗어나면 활성 데이터·표시·충돌을 해제한다. 64 m 청크는 서버 저장·검색에만 사용하며, 일반 플레이의 집 청크 로드·거리별 보관 정책은 제거한다.

2층(`floor_level=1`) 창문을 열면 해당 집을 구독하는 지상(`floor_level=0`)과 다른 주택층 사용자도 변경을 받는다. 직접 조작할 때의 층·근거리 검사는 별도로 유지한다. 웹은 숨긴 상층 메시의 상태도 갱신해 다시 표시할 때 반영한다.

### 3.4 플레이어·몬스터 이동

각 끝점의 공간 조건과 거리를 따로 검사하고, 수신자 합집합에서 중복을 제거한다.

```text
시작점_근처 = 공간이 호환됨(viewer, from.space)
              AND 거리_XZ(viewer.position, from.position) <= R
목표점_근처 = 공간이 호환됨(viewer, to.space)
              AND 거리_XZ(viewer.position, to.position) <= R
이동_전달 = 시작점_근처 OR 목표점_근처
```

| 메시지 | 판정할 구간 |
| --- | --- |
| `PlayerMoved` | 서버의 직전 확정 위치 → 패킷의 새 `position` |
| `MonsterMoved` | 패킷의 `position` → `target_position` |
| 앞으로 이동 의도를 전달하는 경우 | 현재 위치 → 서버가 승인한 현재 구간 목표 |

목표점은 해당 패킷의 이동 구간 끝점이다. 장거리 최종 목적지나 미시작 경유지는 포함하지 않는다. 순간이동은 양 끝의 공간에 알리고 중간을 걷는 애니메이션으로 재생하지 않는다.

두 끝점이 모두 R 밖이면 선분 중간이 반경을 통과해도 이 조건에는 포함되지 않는다. 그런 이동까지 한 패킷으로 지원하려면 선분 교차 판정이 필요하다.

### 3.5 던전 문·소품

문은 개구부 중앙, 소품은 배치물 중심의 월드 좌표를 사용한다. 문 회전이나 조작자 위치에 따라 기준점을 바꾸지 않는다. 입구 마커와 실제 문 좌표가 다르면 실제 문 좌표를 사용하며, 서버·웹·agent-client가 같은 레이아웃으로 계산한다.

수동 토글·자동 닫힘·소품 파괴와 진입 스냅샷에 같은 판정을 적용한다. 정적 던전 레이아웃 캐시와 동적 문·소품 구독을 분리한다.

### 3.6 조경·지형 타일

타일의 실제 영역까지 최단거리에 R을 적용한다. 예를 들어 64×64 m 타일 중심에서 한 축 방향으로 60 m 떨어진 관찰자는 가장자리와 28 m 거리이므로 구독 대상이다. 타일 전체를 갱신 단위로 사용하며 `R + 64` 보정은 제거한다.

건축으로 바뀐 높이·나무·풀도 각 변경 타일의 구독자에게 전달한다. 집 원점이나 조작자 위치는 기준으로 사용하지 않는다. 조작자의 작업 결과는 직접 응답하며, 원거리 작업이 해당 타일의 구독을 뜻하지는 않는다.

프로토콜 83의 WebSocket `TerrainTileVersion`은 타일 좌표와 `files` 목록을 전달한다. 높이·스플랫·나무·풀·조경 파일 각각의 상대 경로와 원본 SHA-256이며, 없는 파일은 `null`이다. 지형 본문을 MessagePack으로 직렬화하거나 전송용 묶음 파일로 복제하지 않는다.

- nginx가 `GET /api/terrain/files/{path}`로 서버가 사용하는 원본을 직접 제공한다. 바뀔 수 있는 경로이므로 HTTP 캐시는 `no-store`이고 ETag에 의존하지 않는다.
- 웹은 메모리 캐시와 별도 지형 Cache Storage를 출처·내용 해시로 조회한다. 없을 때만 다운로드하고 실제 해시를 확인한다. 모델·BGM과 캐시 코드를 공유하지만 배포 목록과 용량 관리는 분리한다.
- 조경 파일이 있으면 그 안의 스플랫을 선택하고 원본 스플랫 다운로드는 생략한다. 나무·풀은 원본과 조경 제거 마스크를 웹에서 함께 적용한다. 원격 에이전트는 높이·지면 재질에 필요한 파일만 받는다.
- 화면 초기 로딩처럼 아직 구독 목록이 없으면 `GET /api/terrain/manifest/{x}/{z}`로 같은 파일 목록만 조회한다.
- 건축·조경·풀 저장·지역 삭제는 원본을 원자적으로 저장한 뒤 해당 타일의 해시 메타데이터를 갱신하고 변경 알림을 게시한다. 기존 `world_edit_guard`와 구독·revision·sequence·권한 판정은 유지한다.
- 파일 다운로드 도중 원본이 바뀌면 해시 불일치 또는 404가 날 수 있다. 웹·에이전트는 최신 목록을 재동기화한다. 이전 해시의 원본을 서버에서 보존하지 않는다. 일시적 오류는 재시도하고, 늦은 응답은 현재 구독·세대·revision을 다시 확인한다.
- 타일 적용에 필요한 파일이 모두 검증되기 전에는 이동을 대기한다. 파일이 없는 타일은 기존 기본 높이·스플랫을 클라이언트에서 생성한다.

배포 준비 명령은 `cargo run --release -p onlinerpg-terrain --bin terrain-manifests -- data/terrain`이다. `TERRAIN_DIR/manifests/{x}/{z}.json`에 작은 해시 목록과 원본 크기·수정 시각을 저장한다. 런타임은 첫 타일 조회에서 이를 확인하고 이후 메모리 목록을 재사용한다. 프로세스 외부에서 원본을 바꾸면 서버를 재시작한다. 크기·수정 시각까지 보존한 교체는 해당 메타데이터도 제거한다. 기존 `snapshots/` 본문은 더 이상 사용하지 않으며 자동 삭제하지 않는다. 배포·캐시 계약은 [TERRAIN_STATIC_SERVING.md](TERRAIN_STATIC_SERVING.md)를 따른다.

| 상태 | 갱신 시점 |
| --- | --- |
| 구독 중 새 revision 수신 | 해당 버전을 캐시에서 읽거나 HTTP로 즉시 요청. 적용 완료까지 이동 대기 |
| 범위 밖 캐시를 가지고 재진입 | `Enter`의 버전과 일치하는 본문만 활성화 |
| HTTP 응답 전에 이탈·재진입·새 세대·버전 변경 | 이전 요청 결과는 현재 구독에 적용하지 않음 |
| `404` 또는 이전 서버의 `409` 수신 | `ResyncWorld`로 최신 버전 목록 요청 |
| 일시적 HTTP 실패·클라이언트 적용 실패 | 미완료 상태를 유지하고 재시도 |
| 높이 변경 | 높이와 이에 의존하는 나무·풀 배치를 함께 갱신한 뒤 완료 처리 |

HTTP 다운로드는 WebSocket 수신과 분리한다. agent는 다운로드 중 공유 상태 잠금을 잡지 않으며, 적용 시 연결별 세대·활성 타일·revision과 공유 월드 세대를 다시 확인한다. 여러 agent 연결의 공유 높이·재질은 revision이 역행하지 않게 한다. 기존 높이 샘플러의 메모리·세대별 HTTP 캐시도 함께 갱신한다. 내용 해시 캐시는 세대가 바뀌어도 재사용한다.

구독 밖의 렌더링·샘플링용 기존 HTTP 경로는 유지한다. 범위 밖 모든 접속자에게 보내던 무효화 경로는 제거했다. 여러 변경 타일 중 하나의 읽기가 실패해도 나머지 버전은 전달하며, 실패한 타일은 재동기화 요청 및 주기 작업에서 재시도한다.

### 3.7 플레이어 상태와 효과

| 대상 | 전달 기준 |
| --- | --- |
| 장비·횃불·젖음·탑승·칭호·발광·상호작용 자세 | 플레이어 구독자 |
| 악기 시작·음표 | 연주자의 실제 위치·공간과 R. `INSTRUMENT_AUDIBLE_RADIUS` 제거 |
| 낚시 동작·찌·낚싯줄 | 하나의 세션으로 묶어 낚시하는 플레이어의 실제 위치·공간과 R 적용 |
| 투사체 등 독립적인 지속 효과 | 자체 위치·형상과 수명 |
| 즉시 끝나는 공격·피격 효과 | 각 효과의 위치·영역과 R |

낚시는 찌 좌표를 표시용으로 전달하되 찌의 이동으로 구독자를 바꾸지 않는다. 찌만 가깝고 플레이어는 R 밖인 관찰자가 효과를 받지 못하는 경우는 허용한다.

지속 효과는 중간 진입 시 현재 상태를 복원하고, 종료·이탈 시 기존 구독자의 표시·소리를 정리한다. 낚시하는 플레이어가 순간이동하거나 접속을 끊어도 기존 구독자에게 종료가 전달돼야 한다. 과거의 공격·음표는 재생하지 않는다.

효과가 범위 밖 엔티티를 참조하면 표현에 필요한 좌표·공개 정보만 포함하거나 효과를 분리한다. 참조 ID를 받았다는 이유로 그 엔티티를 활성 목록에 추가하지 않는다.

## 4. 상태 수명주기와 순서

| 이전 전송 목록 | 새 판정 | 전달 |
| --- | --- | --- |
| 없음 | 구독 대상 | 최신 전체 상태 `Enter` |
| 있음 | 구독 유지 | 변경 `Update` |
| 있음 | 범위 이탈 | `Leave` |
| 있음 | 월드에서 삭제 | `Delete` |
| 없음 | 구독 대상 아님 | 주변 전달 없음 |

`Leave`는 해당 시야에서의 해제, `Delete`는 월드에서의 삭제다. 몬스터 사망 후 남는 시체도 지속 상태다. 기존 `MonsterSpawned` 같은 메시지를 재사용하더라도 내부 의미는 구분한다.

### 4.1 갱신과 이동 순서

- 로그인·이동·순간이동·리스폰·층 전환 및 대상의 생성·이동·형상 변경 때 관심 집합을 갱신한다.
- 진입 스냅샷에는 현재 문 열림·수량·사망·연주 등 런타임 상태를 포함한다. 빈 목록도 적용해 이전 대상을 정리한다.
- 상태 확정, 관심 집합 등록, 스냅샷 큐 삽입을 묶고 그 뒤 변경을 순서대로 전달한다. 스냅샷에 포함된 revision을 중복 적용하지 않는다.
- 이탈·삭제 수신자는 이전에 그 대상을 받은 사용자다. 새 위치나 반경으로 정리 수신자를 다시 줄이지 않는다.
- 이동을 처음 받는 사용자는 `Enter → 이동`, 이탈하는 사용자는 `마지막 이동 → Leave` 순서로 받는다. 같은 구간이 스냅샷에 포함되면 합칠 수 있다. 웹은 마지막 구간의 화면 내 보간 후 표시를 정리한다.
- 이동 중에는 현재 확정 위치 또는 유효한 현재 구간 목표가 R 안이면 구독을 유지한다. 목표 변경·중단·보정·사망 때 다시 계산하며, 완료·취소된 구간으로 유지하지 않는다.
- 목표점 때문에 미리 받은 대상도 실제 현재 위치로 표시하고 LLM 인지에 포함한다. 목표점을 현재 위치나 공격 가능 거리로 사용하지 않는다.

### 4.2 버전과 복구

| 식별자 | 용도 |
| --- | --- |
| `world_epoch` | 서버 재시작 전후 캐시 구분 |
| 연결별 관심 집합 세대·송신 순번 | 과거 시야 응답과 누락 판별. 이전 구간의 늦은 `Leave`가 새 구독을 지우지 않게 함 |
| 대상별 `revision`과 삭제 표식 | HTTP·여러 agent 연결의 역순 응답, 삭제 후 이전 상태 복원 방지 |

`WorldUpdate`는 `world_epoch`, `generation`, `sequence`, 서버 확정 `position`·`floor_level`, `reset`, `ready`, `events`를 포함한다. 각 이벤트는 `subject`, `revision`, `change`, 기존 메시지 본문 목록을 가진다. 전체 스냅샷은 새 세대의 `sequence=1, reset=true` 한 묶음으로 적용하며 빈 스냅샷도 이전 시야를 비운다. `ready=false` 또는 타일 적용 대기 중에는 이동을 진행하지 않는다.

요청 실패를 빈 정상 상태로 처리하지 않는다. 재연결 시 시야를 비우고 새 스냅샷을 받으며, 스트림 누락이나 복구가 필요한 순서 불일치는 재동기화한다.

`ResyncWorld`는 새 세대의 전체 스냅샷을 요청한다. 이미 처리한 순번·과거 세대는 무시하고, 실제 순번 누락·revision 역행은 재동기화를 요청한다. 서버 재시작 전 세대는 다시 활성화하지 않는다. 여러 agent 연결이 공유 본문을 모두 해제한 뒤 오래된 `Enter`만 받은 경우에도 최신 스냅샷을 요청한다.

## 5. 구현 구조

### 5.1 서버의 공통 전달 계층

`server/src/game_state/interest.rs`에 공통 판정, 연결별 전송 목록, 대상별 구독자, 순서 있는 송신 큐를 둔다. 기능 코드는 상태·위치·형상을 제공하고 이 계층이 수신자를 결정한다.

```text
reconcile_view(player_id)
reset_world_view(player_id)
publish_subject_change(message)
publish_nearby(position, floor, message, skip)
publish_house(house) / remove_house_subject(house_id)
publish_terrain_tiles(tiles)
send_direct_message_to_players(player_ids, result)
```

| 구현 | 역할 |
| --- | --- |
| [interest.rs](../server/src/game_state/interest.rs) | 공간 인덱스, 연결별 집합과 역구독 목록, 이동·효과·낚시, 순번과 재동기화, 타일 전달 |
| [interest_subjects.rs](../server/src/game_state/interest_subjects.rs) | 플레이어·몬스터·아이템·영지 시설·지속 효과의 최신 진입 상태와 정리 메시지 |
| [interest_dungeon.rs](../server/src/game_state/interest_dungeon.rs) | 실제 개구부·배치 좌표에 따른 문·소품 개별 구독 |
| [messages.rs](../shared/src/messages.rs) | `WorldUpdate`, `ResyncWorld`, 메시지별 `DeliveryClass` 분류 |

내부 `publish_player_movement`·`publish_monster_movement`는 끝점 합집합을, `publish_effect`는 효과 영역을 사용한다. `publish`·`remove`가 대상 revision과 기존 구독자 정리를 담당한다. 상태 확정과 게시를 같은 기능 잠금 범위에서 실행하고, 관심 집합 변경과 큐 삽입은 관심 계층 잠금 안에서 처리한다. 집·지형의 파일 변경과 스냅샷 읽기는 `world_edit_guard`로 직렬화한다.

주변 전달 API는 반경 인자를 받지 않는다. `player_ids_within_position(..., radius)` 같은 조회는 공격·상호작용·AI 내부 판단에만 사용한다. `ServerMessage`는 §1의 분류를 명시하며 미분류 메시지를 자동 전역 전송하지 않는다.

집을 포함한 동적 객체는 WebSocket 스냅샷으로 전달한다. 어드민 REST 변경도 공통 계층을 거친다. 편집용 집 HTTP API는 유지할 수 있지만 그 응답을 일반 플레이의 활성 상태에 직접 적용하지 않는다.

### 5.2 웹과 agent-client의 상태·캐시

- 웹의 표시와 WASM 충돌은 같은 활성 동적 목록을 사용한다. 정적 배치 충돌 위에 해당 목록의 동적 충돌을 합친다. 모델·텍스처 캐시는 메모리 예산에 따라 재사용할 수 있다.
- agent의 연결별 `WorldView`는 활성 대상 ID·공간·동기화 상태를, `WorldCache`는 revision이 있는 공유 본문·정적 데이터를 가진다. 여러 연결의 본문은 대상 revision으로 병합하며 연결별 세대는 서로 비교하지 않는다.
- agent A가 떠나도 B가 사용하는 집 본문은 유지한다. 사용하는 활성 시야가 없어지면 공유 집 런타임 데이터도 해제한다. 다른 연결의 대상이 자신의 주변 목록에 포함되지 않게 한다.
- 집의 웹 `LOAD_RADIUS`·`EVICT_RADIUS`·청크 HTTP 요청과 agent의 `fetch_houses_around`·`fetched_house_chunks`를 제거한다.
- 정적 가구의 `fetch_furniture_around`·`fetched_furniture_regions`와 먼 스케줄 목적지의 정적 데이터 사전 로드는 유지한다. 버전 변경은 §1의 캐시 정책을 따른다.
- 장거리 경로는 잠정 경로로 두고 접근하면서 최신 동적 충돌로 다음 구간을 갱신한다. 스냅샷 대기 영역은 미확인 상태로 두며, 검증된 영역까지 이동한 뒤 갱신되면 이어간다. 서버 위치 보정을 우선 적용하고, 보정이 반복되면 재동기화해 경로를 다시 계산한다.

웹의 [worldView.ts](../client/src/lib/network/worldView.ts)와 agent의 shared `WorldView`는 서버 확정 위치에서 `R-1 m`까지 다음 이동을 허용한다. 1 m는 다음 충돌 확인을 위한 여유다. 지형 적용이나 정적 가구 재로드가 대기 중이면 웹 이동을 멈추고 경로를 보존한다. agent도 타일 대기 중에는 경로 지점을 소비하지 않으며, 공유 충돌 revision이 바뀌면 경로를 다시 계산한다. 웹의 이탈 정리는 마지막 이동 보간 후 실행하고 세대·재진입 여부를 다시 확인한다.

### 5.3 agent 인지

LLM 상태 목록·이벤트·대상 검색은 활성 `WorldView`를 사용한다. 이미 받은 동적 대상을 27 m나 현재 위치 중심의 원으로 다시 거르지 않는다. 발견 알림은 활성 대상의 진입·퇴장에 연결하고 ID·구독 세대로 중복을 억제한다. 기존 27 m 진입/`27 + 5 m` 망각 범위는 제거한다.

넓게 캐시한 정적 데이터의 주변 정보 선택과 지형 요약·시야 표시에는 공통 R을 사용한다. 거리·방향 표시, 정렬, 요약, 중복 이벤트 병합은 유지할 수 있다.

이관 범위는 [perception.rs](../agent-client/src/state/perception.rs), [world_state.rs](../agent-client/src/state/world_state.rs), [events.rs](../agent-client/src/state/events.rs)의 몬스터 이동 필터, [social.rs](../agent-client/src/state/social.rs)·[movement.rs](../agent-client/src/state/movement.rs)의 대상 검색, [prompt.rs](../agent-client/src/driver/prompt.rs)다.

## 6. 새 기능 구현 절차

1. **분류:** §1의 메시지 분류에 따라 전달 성격을 정한다.
2. **대상 정의:** §3에 따라 동기화 단위, 위치·형상·공간을 정한다. 이동은 패킷의 시작점·목표점을 명시한다.
3. **전달 연결:** §5.1의 공통 API를 사용한다. 기능 내부에 별도 반경이나 수신자 계산을 추가하지 않는다.
4. **수명주기:** 지속 상태에는 진입 스냅샷, 변경, 종료·삭제·이탈을 구현하고 §4의 순서·revision을 적용한다.
5. **클라이언트:** 웹 표시·충돌과 agent 상태·인지에 같은 진입·갱신·정리를 연결한다.
6. **검증:** §8에서 해당되는 경계·재진입·삭제·공유 캐시 시나리오를 검증한다.

## 7. 이관과 성능

### 7.1 완료한 이관 범위

| 단계 | 작업 |
| --- | --- |
| 1. 공통 기반 | shared 판정·WASM 상수 API, 관심 집합·송신 순서·버전, 메시지 분류 도입 |
| 2. 집 | 생성·수정·철거·문·창문을 집 구독으로 이관. 스크롤 건축·어드민 삭제 포함. `apply_open_door_state`가 반영된 스냅샷과 §5.2 캐시 구조 적용 |
| 3. 엔티티·효과 | 기존 AOI 대상, 이동 끝점 판정, 던전 문·소품, 악기·낚시를 §3~4로 이관. agent 인지는 §5.3 적용 |
| 4. 지형·기존 경로 제거 | 타일 revision·즉시 재수신·agent 디스크 캐시 갱신 완성 후 전역 무효화와 개별 주변 송신 경로 제거 |

### 7.2 기존 상수의 다른 용도

- `NPC_SIGHT_RADIUS`를 제거했다. [deals.rs](../server/src/game_state/deals.rs)의 거래 제안 허용 거리와 [walk.rs](../agent-client/src/driver/walk.rs)의 이동 제한은 별도 행위 규칙으로 유지한다.
- `EVENT_DELIVERY_RADIUS`는 [monster_ai.rs](../server/src/game_state/monster_ai.rs)의 활성 AI 조회, [ambient_spawn.rs](../server/src/game_state/ambient_spawn.rs)의 스폰 제약, 몬스터 소유권, 던전 입구 조작 검사에도 쓰인다. 시야와 연동할 항목은 함께 검증하고 조작 거리 등은 용도에 맞게 분리한다.

### 7.3 성능과 배포

현재 [카메라 설정](../client/src/lib/components/game-scene/camera-utils.ts)의 기본 각도·평지·최소 일반 줌에서 32:9 화면 모서리는 약 39.6 m까지 보인다. 이 경우도 §1의 가장자리 정책을 따른다. 균일한 밀도에서 점 이벤트 수신자 수는 대략 R의 제곱에 비례한다. 32→64 m 확대 시 약 4배이며, 실제 부하는 대상 크기와 사용자 분포에 따라 측정한다.

- 기존 공간 인덱스를 활용하고, 움직인 관찰자·대상 주변만 갱신한다.
- 집·타일·울타리는 실제 영역이 걸친 셀에 등록하고 월드 순환을 처리한다.
- 같은 게시의 이벤트 본문을 한 번 직렬화해 수신자 간 공유하고, 연결별 봉투에 순번·세대를 붙인다. 차단된 좌판 문구는 해당 수신자에 맞게 직렬화한다.
- `world_delivery` trace는 연결별 송신 바이트, 활성 대상 수, 전체 스냅샷 여부를 기록한다. 프로덕션 수신자 분포·스냅샷 지연·이동·AI 처리 시간의 이관 전후 부하 비교는 배포 전 별도 측정 항목이다.

[shared/src/lib.rs](../shared/src/lib.rs)의 정책에 따라 메시지 형태·의미가 바뀌면 `PROTOCOL_VERSION`을 올린다. 웹 WASM·서버·agent-client를 호환되는 버전으로 함께 배포 준비하고, 원격 agent와 포크에 변경을 안내한다. 롤백도 세 구성요소의 버전을 맞춘다. 위 단계는 개발·검토 단위이며 구형 클라이언트가 새 의미를 잘못 처리하는 중간 상태는 배포하지 않는다.

공통 전달 계층은 프로토콜 **79 → 80**, 지형 본문의 HTTP 분리는 **80 → 81** 변경이다. 서버·웹 WASM·agent-client를 함께 갱신해야 한다. 이 작업에서 프로덕션 배포는 실행하지 않았다.

## 8. 검증 기준

검증은 shared 판정, 서버 수신자·수명주기, 웹·agent 상태 적용으로 나누고 같은 데이터로 확인한다. 거리 경계는 R에서 파생한다.

| 시나리오 | 기대 결과 |
| --- | --- |
| `R-ε`·`R`·`R+ε`, X 순환·음수 좌표 | 경계 포함과 진입·퇴장 판정 일치 |
| 다른 층·다른 던전의 같은 깊이 | 대상별 공간 정책 준수 |
| 조작자는 멀고 대상은 가까움 / 그 반대 | 대상의 구독 규칙으로 수신자 결정 |
| 집 원점은 멀지만 방은 가까움, 회전·청크 경계·같은 청크 안 이동 | 실제 바닥 영역으로 집 전체 구독·해제 |
| 집 위층에서 철거 관찰, 2층 창문을 지상·다른 주택층에서 관찰 | 집 구독자 모두 변경 수신. 다른 층 생물은 미공개 |
| 숨긴 상층 메시의 창문 변경 후 외관 표시 | 최신 상태 표시 |
| 문이 열린 상태로 이탈한 뒤 닫힘·복귀, 범위 밖 동적 대상 생성·수정·철거 | 최신 스냅샷과 충돌 적용. 삭제된 대상 잔존 없음 |
| 정적 가구 재방문 / 데이터 버전 변경 배포 | 같은 버전은 재사용, 변경 시 캐시·다운로드 기록·충돌 갱신 |
| 던전 입구 마커와 실제 문 위치가 다름, 자동 닫힘, 원거리에서 문·소품 변경 후 접근 | 실제 대상 위치 판정과 최신 상태 복원 |
| 관찰자 또는 몬스터 이동, 시작점만·목표점만·둘 다 R 안 | 등장 후 이동 전달. 수신자 중복 없음 |
| 현재 위치는 R 밖, 목표점은 R 안 | LLM 인지 포함. 실제 현재 위치와 목표 구별 |
| 두 끝점 모두 R 밖 | 끝점 OR 조건의 수신자는 아님. 기존 구독자의 종료·퇴장 정리는 전달 |
| 목표 변경·중단·보정·사망, 마지막 이동 후 이탈 | 이동·종료 갱신 뒤 퇴장. 이전 구간의 응답이 새 구독을 제거하지 않음 |
| 순간이동·집 위치 변경 직후 삭제 | 이전 구독자 정리와 새 구독자 스냅샷·삭제 순서 보장 |
| 늦은 HTTP·여러 agent 연결의 역순 응답·삭제 후 응답 | revision 역행·삭제 대상 복원 없음 |
| agent A 이탈, B는 집 사용 / A 재진입 | B 충돌 유지, A 최신 상태 복원, 시야 간 대상 혼입 없음 |
| 기존 27 m 밖의 대상·이벤트, 원거리 공유 캐시 | 동적 대상은 활성 시야 사용, 정적 주변 정보는 공통 R 적용 |
| 연주·낚시·모닥불·시체 중간 진입과 종료 | 현재 상태 복원과 종료·이탈 정리. 과거 일회성 효과 재생 없음 |
| 기존 30 m 밖에서 악기 관찰 | 시작·음표 수신과 종료 정리 |
| 낚시 플레이어만 가까움 / 찌만 가까움, 찌 이동·플레이어 순간이동·접속 해제 | 플레이어 기준 구독, 실제 찌 좌표 표시, 기존 구독자의 종료 정리 |
| 타일 중심은 멀지만 가장자리는 가까움 / 모서리에서도 R 밖 | 실제 타일 영역으로 구독 결정 |
| 조작자·집 원점은 멀지만 변경 타일은 가까움 | 해당 타일 구독자 갱신, 작업 결과는 조작자에게 응답 |
| 정지 관찰자의 타일 revision 수신 / 원거리 캐시로 재진입 | 버전에 맞는 캐시·HTTP 본문 적용 / 최신 버전으로 교체 |
| agent에 이전 높이 메모리·디스크 캐시가 남음, 갱신 요청 실패 | 최신 높이·종속 배치로 갱신. 실패 상태를 완료로 처리하지 않음 |
| 접속 해제·송신 누락·재시작·스냅샷 실패 | 재동기화 |
| 화면비·카메라 회전·고저차·일반 줌아웃 변경 | 32 m 정책 유지. 가장자리 현상 허용, 구독 진입 시 최신 상태 복원 |

완료 기준은 서버의 관심 집합과 웹·agent-client의 활성 상태가 일치하고, 재진입 시 최신 상태가 복원되는 것이다. 화면 전체 동기화는 요구하지 않는다.

### 8.1 자동 검증

- shared: 형상 경계·X 순환·집의 방 합집합·던전 공간 분리, 실제 문 개구부 좌표, 세대·순번 누락과 복구.
- 서버: 이전 구독자 삭제·빈 이탈·끝점 이동 순서·중간 진입 연주와 낚시, 집·던전·지형의 기존 기능 회귀, 일부 타일 읽기 실패 후 정지 상태 재시도.
- agent: 공유 집·던전 참조 수명, 역순 revision·삭제 표식·과거 세대, 본문 해제 후 오래된 진입, 제어권과 활성 목록 분리, 타일 대기 중 경로 보존, 늦은 HTTP 높이 응답과 디스크 캐시.
- 웹: 세대·순번·타일 적용 대기, 서버 주도 집 활성 목록, 이동 대기 중 경로 보존, 새 월드의 정적 가구 충돌 해제와 HTTP 응답 역전·재시도.

검증 명령: `cargo fmt --all`, `cargo check --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test -p onlinerpg-server -p onlinerpg-shared -p onlinerpg-terrain -p agent-client`; `client`에서 `npm run build:wasm`, `npm run check`, `npm run lint`, `npm test`.

2026-09-15 실행 결과: 서버 968개, shared 412개, terrain 52개, agent-client 296개, 웹 1,022개 통과. 기존 무시 테스트는 Rust 6개다. `cargo check`, 웹 타입 검사·린트, WASM 빌드도 통과했다. 생성한 WASM으로 프로토콜 80·공통 반경·`WorldUpdate`의 타일 본문·`ResyncWorld`를 직접 직렬화/역직렬화해 형식 일치를 확인했다.

전체 실행 중 기존 통계 테스트 `weapon_enchant_leaderboard_ranks_inventory_maxima_and_returns_history`가 타임스탬프 중복 제약으로 한 차례 실패했으며, 단독 재실행과 이후 전체 실행은 통과했다.

자동 검증 결과와 프로덕션 부하 측정은 구분한다. 위 경계 회귀는 자동 테스트로 확인하며, 화면비별 수동 플레이 및 실제 접속자 부하 비교는 별도 운영 검증 항목이다.

2026-09-16 지형 HTTP 분리 검증: 서버 982개, shared 418개, terrain 52개, agent-client 299개, 웹 1,047개 통과. Rust의 기존 무시 테스트 6개는 유지했다. `cargo fmt`, `cargo check --workspace --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, 웹 타입 검사·린트, WASM 빌드, nginx 설정 검사도 통과했다. 실제 WASM 디코더로 프로토콜 81의 버전 알림·HTTP 스냅샷 형식과 선택 필드 처리를 확인했다. 새 테스트는 불변 버전·ETag·변경 시 409·읽기 실패 시 503, ground 프로필, 재시작 캐시 재사용·손상 검출·요청 공유, 늦은 응답·이탈·세대 변경·재시도를 검증한다. 운영 배포 및 실제 트래픽 감소 측정은 수행하지 않았다.
