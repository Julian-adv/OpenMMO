# 배포 메모

## 2026-09-07 완료: 마을 꾸미기 데이터 이관

당초 2026-09-08 예정이었으나 사용자 요청으로 2026-09-07 11:17 KST에 배포했다.
운영 커밋은 `635ab299d7cff95ec3aa21253efa5f374586d357`이며, 프로토콜 59용
`agent-client-v0.44.0` Linux·Windows 패키지를 같은 커밋으로 공개했다.
아래 이관은 완료됐으므로 다음 배포에서 다시 실행하지 않는다.

- [x] **펜스**: 마을 관리자 목책 271개를 이관했다. 범위는 X `-1555..-1432`,
  Z `4704..4823`이며, 운영 관리자 `jake1`의 캐릭터 ID 28에 `installer_id`를 연결했다.
  새 서버와 동일한 스키마 변경 및 삽입을 한 트랜잭션으로 처리했다. 기존 운영 목책
  5,313개는 모두 보존됐으며, DB 무결성과 외래 키 검사를 통과했다.
- [x] **새 집**: `data/housing/r-23_+74/r-23_+74_3.json`과
  `r-23_+74_4.json`을 추가하고, `r-23_+73/r-23_+73_2.json`의 지붕 방향을 반영했다.
  `r-23_+74_2.json`은 내용이 같아 복사하지 않았고 운영 백업 파일도 보존했다.
- [x] **새 바닥·가구**: 지형 후보 21개를 내용 비교해 실제 변경 20개만 복사했다.
  `r-02_+04`의 타일 `(-22,74)`, `(-22,75)`, `(-23,74)`, `(-23,75)`에 대한
  풀·높이·splat·landscaping 각 4개, 원본 높이 2개, objects 1개, trees 1개다.
  land-grades는 내용이 같아 제외했다. 운영에만 있는 가구가 없음을 확인했다.
- [x] **Rowan**: 기존 운영 NPC 설정을 보존하면서 `estate_architect` 항목을 추가했다.
  낮 위치 `(-1450.0, 1.1, 4761.8)`, 회전 `-91.7°`, 밤 71번 침대와 바닥 샘플
  안내가 반영됐다. 판매 견본집 안내를 실제 판매 목록인 6종으로 수정했다.
  운영 로그에서 Rowan 접속과 71번 침대 취침 일과 진입을 확인했다.
- [x] **배포 검증**: 두 서비스 `active`, 충돌 캐시 준비와 서버 시작 완료 로그,
  한국어·영어 공지 API, 새 모델 2개의 공개 URL 및 `assets.lock` 체크섬을 확인했다.
  집 3채와 침대 71의 API 응답, landscaping 4개 타일의 공개 splat 데이터도 검증했다.
- [ ] **남은 수동 확인**: 인게임 화면에서 마을 목책 충돌·회수와 집·가구·바닥
  견본의 외관을 직접 확인한다. 이번 배포에서는 DB·API·로그 검증까지 완료했다.

### 백업과 이관 기록

운영 호스트 `/home/ubuntu/deploy-20260907-v044/`에 다음을 보관했다.

- `backup/game_data.db`: 서비스 정지 후 이관 직전의 SQLite 백업.
- `backup/data/`: 덮어쓴 집·지형 파일 원본.
- `backup/agent-config.toml`: Rowan 추가 전 운영 NPC 설정.
- `manifest.json`: 복사한 23개 파일의 경로와 이전·이후 SHA-256.
- `migrate-town-fences.sql`: 관리자 목책 271개의 좌표, 소유자 검증, 보존 검사.
- `operator-state-applied`: 이관 완료 표시.

게임 빌드를 모두 마친 뒤 서비스를 정지하고 백업·이관·검증을 수행한 후 재시작했다.
공지 파일은 `data/announcements/2026-09-07-landscaping-rowan.md`다.
로컬 개발 영지의 목책 100개는 운영의 다른 플레이어 소유지와 겹쳐 제외했다.
개발 영지 소유권·캐릭터·가방·골드·세금 상태는 이관하지 않았다.

집·지형·DB는 Git 배포만으로 전달되지 않는 운영 데이터다. 이번 마을 펜스 이관은
사용자가 요청한 예외이며, 일반 배포에서 개발 플레이어 상태를 옮기지 않는 원칙은 유지한다.
