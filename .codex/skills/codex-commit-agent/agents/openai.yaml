interface:
  display_name: "Codex Commit Agent"
  short_description: "Run checks and commit requested changes safely"
  default_prompt: "Use $codex-commit-agent to validate and commit the current changes."
